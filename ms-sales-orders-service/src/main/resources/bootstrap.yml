spring:
  application:
    name: sales-order-service
  cloud:
    bus: 
      enabled: true
    config: 
      #uri: ${CONFIG_URL}
      uri: https://ms-config-server.herokuapp.com
server: 
  port: ${PORT:8100} 