management:
  endpoint:
    bus-refresh:
      enabled: true
    env:
      enabled: true
    metrics:
      enabled: true
    configprops:
      enabled: true
    health:
      enabled: true
  endpoints: 
    web:
      exposure:
        include:
        - info
        - health
        - env
        - configprops
        - metrics
        - refresh